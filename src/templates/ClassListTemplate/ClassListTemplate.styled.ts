import styled from 'styled-components';

export const StyledClassListTemplate = styled.div`
  padding: 3rem;
`;

export const StyledMystudyListSection = styled.section`
  .study-block {
    margin-bottom: 1.8rem;
  }

  .button-wrapper {
    margin: 3rem 0;
    text-align: center;
  }
`;

export const StyledTitle = styled.h1`
  margin-bottom: 2.7rem;
  font-weight: 700;
  font-size: 2rem;
`;