import styled from 'styled-components';

export const StyledFindAuthTemplate = styled.div`
  padding: 2.5rem 0;
  position: relative;
`;

export const StyledHeaderSection = styled.section`
  margin-bottom: 2.5rem;
`;

export const StyledFindFormSection = styled.section`
  @media (max-width: 769px) {
    margin: 0 2rem;
  }
`;
