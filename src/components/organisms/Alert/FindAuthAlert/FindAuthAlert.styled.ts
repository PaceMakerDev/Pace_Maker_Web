import styled from 'styled-components';

export const StyledFindAuthAlert = styled.div`
  padding: 1rem 0;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;

  .highlight-message {
    margin-bottom: 1.25rem;
    line-height: 1.5;
  }

  .basic-message {
    margin-bottom: 3rem;
  }
`;